import { RobotStatusPipe } from './robot-status-pipe';

describe('RobotStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new RobotStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
